<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fishrequirementsystem">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FishRequirementSystem"
        tools:targetApi="31">
        <activity
            android:name=".AddActivity"
            android:exported="false" />
        <activity
            android:name=".Time_Schedule"
            android:exported="false" />
        <activity
            android:name=".FishInfo"
            android:exported="false" />
        <activity
            android:name=".Feeding"
            android:exported="false" />
        <activity
            android:name=".PHCheckActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".classes.AlarmReceiver" />

        <service
            android:name=".classes.Music"
            android:enabled="true" />

    </application>

</manifest>